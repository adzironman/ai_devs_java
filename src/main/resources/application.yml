spring:
  application:
    name: aidev
  ai:
    openai:
      api-key:
client:
  openaimoderationapi:
    url: https://api.openai.com/v1
  aidevs:
    url: https://tasks.aidevs.pl
    apiKey:

feign:
  client:
    config:
      default:
        connectTimeout: 600000
        readTimeout: 600000

task: helloapi
